// showing objects keys and values

// create an object
const mobile = {
    Name: 'Tecno B2P',
    Model: 'POP5',
    Storage: '32GB',
    version: 'Normal'

};

console.log(`${Object.keys(mobile)}\n${Object.values(mobile)}`);
